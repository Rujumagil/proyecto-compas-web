import('./clean-balance.js?v=1');
(() => { const progress=document.querySelector('.progress'), glow=document.querySelector('.cursor-glow'); const update=()=>{ if(progress){const max=document.documentElement.scrollHeight-innerHeight; progress.style.setProperty('--scroll',`${max>0?(scrollY/max)*100:0}%`)}}; update(); addEventListener('scroll',update,{passive:true}); if(glow&&matchMedia('(pointer:fine)').matches){let x=innerWidth/2,y=innerHeight/2,tx=x,ty=y; addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY},{passive:true}); const loop=()=>{x+=(tx-x)*.08;y+=(ty-y)*.08;glow.style.left=`${x}px`;glow.style.top=`${y}px`;requestAnimationFrame(loop)};loop()} document.querySelectorAll('[data-tilt],.glass-card,.orbit-node').forEach(el=>{if(!matchMedia('(pointer:fine)').matches)return;el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),px=(e.clientX-r.left)/r.width-.5,py=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${py*-4}deg) rotateY(${px*6}deg) translateY(-4px)`});el.addEventListener('pointerleave',()=>el.style.transform='')}); document.querySelectorAll('.button,.nav-cta').forEach(el=>el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--mx',`${e.clientX-r.left}px`);el.style.setProperty('--my',`${e.clientY-r.top}px`)})); })();